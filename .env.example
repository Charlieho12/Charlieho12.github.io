# Example environment file for Portfolio Contact Form (SendGrid)
# Copy to .env and fill values

PORT=4000
CORS_ORIGIN=http://localhost:4000,http://localhost:5500

# SendGrid
SENDGRID_API_KEY=YOUR_SENDGRID_API_KEY_HERE
SENDGRID_FROM=verified-sender@example.com
# Optional override for destination (defaults to SENDGRID_FROM)
CONTACT_TO=verified-sender@example.com

# Optional debug flag (1 enables extra env diagnostics in function dry-run)
# CONTACT_DEBUG=1
